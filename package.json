{
  "name": "kaist-bus-next",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "update-data": "python scripts/kaist_scrape.py",
    "build": "vite build",
    "build:new": "vite build --base=/new/ --outDir=new",
    "deploy": "npm run update-data && npm run build",
    "deploy:new": "npm run update-data && npm run build:new",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
