{
  "name": "kaist-bus-next",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "update-data": "python scripts/kaist_scrape.py",
    "build": "vite build && node scripts/legacy-redirects.cjs --out dist",
    
    "deploy": "npm run update-data && npm run build",
    
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
